# ── Core ML ────────────────────────────────────────────────
torch>=2.2.0
torchvision>=0.17.0
timm>=0.9.12
Pillow>=10.0.0
numpy>=1.26.0
pandas>=2.1.0

# ── Training utilities ──────────────────────────────────────
scikit-learn>=1.3.0
grad-cam>=1.4.8          # pytorch-grad-cam
wandb>=0.17.0
seaborn>=0.13.0
matplotlib>=3.8.0

# ── API & Serving ───────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.0

# ── Streamlit UI ────────────────────────────────────────────
streamlit>=1.33.0
streamlit-extras>=0.4.0

# ── RAG + Agent (Phase 3) ───────────────────────────────────
langgraph>=0.1.0
langchain>=0.2.0
langchain-text-splitters # Added for document chunking
langchain-anthropic>=0.1.0
google-genai             # Added for Gemini API support
langchain-google-genai   # Added for LangChain Gemini integration
qdrant-client>=1.9.0
sentence-transformers>=2.7.0
pymupdf                  # Added for PDF extraction (fitz)

# ── Data Scraping & Parsing (Phase 3 Extra) ────────────────
beautifulsoup4           # Added for HTML parsing
lxml                     # Added as a fast parser for BeautifulSoup
biopython                # Added for PubMed API (Entrez)

# ── Weather API (Phase 3) ───────────────────────────────────
requests>=2.33.1         # Updated to match current project version
httpx>=0.27.0

# ── Utilities ───────────────────────────────────────────────
cachetools>=7.1.1        # Added for memoization and caching
pyyaml>=6.0.1
python-dotenv>=1.2.2     # Updated to match current project version
tqdm>=4.66.0
rich>=13.7.0

# ── Testing & Mocking ───────────────────────────────────────
pytest>=7.4.0
responses>=0.26.2        # Added for mocking HTTP requests in agent tests